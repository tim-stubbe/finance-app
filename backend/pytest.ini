[pytest]
# Harte Obergrenze pro Test (pytest-timeout). Kein einzelner Test soll die
# Suite/CI blockieren koennen - blockierende Aufrufe (Netz, Streams, WebSocket)
# scheitern damit sichtbar statt haengen zu bleiben.
timeout = 60
timeout_method = thread
