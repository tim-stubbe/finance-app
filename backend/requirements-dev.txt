# Nur für Tests/CI - bewusst NICHT in requirements.txt (das Produktions-
# Image, siehe Dockerfile, soll schlank bleiben, Tests laufen nie im
# laufenden Container mit).
-r requirements.txt
pytest==9.1.1
httpx==0.28.1
# Harte Obergrenze pro Test - ohne die haengt ein blockierender Test (z.B. ein
# nie zurueckkommender Netz-/Stream-Aufruf) die ganze CI. Siehe pytest.ini.
pytest-timeout==2.4.0
# Sprach-Ein-/Ausgabe (requirements-voice.txt) ist hier NICHT eingebunden:
# die Kette (onnxruntime/scipy/ctranslate2 ...) hat auf python:3.14 in CI
# keine vollstaendigen Wheels und brach den Lauf ab. Die Voice-E2E-Tests
# ueberspringen sich sauber, wenn die Pakete fehlen (siehe test_voice.py,
# _HAS_WHISPER / KIES_VOICE_E2E). Lokal bei Bedarf:
#   pip install -r requirements-voice.txt
# numpy allein ist klein und hat verlaessliche cp314-Wheels - reicht fuer
# den Frame-Slicing-Unit-Test von voice/wakeword.py (test_voice.py).
numpy==2.5.2
